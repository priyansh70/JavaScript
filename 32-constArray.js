// Use Const for Creating Array
const numArr = [12,62,45,68,12];
console.log(numArr);

numArr.push(0);
console.log(numArr);

// this is constant type of array so we can't change values of array 

// numArr = [10,20,30,40]; 
// console.log(numArr);

// Prefer const for create array 